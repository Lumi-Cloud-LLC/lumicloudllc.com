apiVersion: v1
kind: Service
metadata:
  labels:
    app: frontend-web
  name: frontend-web
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: frontend-web
  type: ClusterIP
status:
  loadBalancer: {}
